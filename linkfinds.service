[Unit]
Description=linkfinds
After=network.target
[Service]
ExecStart=/opt/linkfinds/linkfinds-responder.js
Restart=always
User=bot
Group=bot
Environment=PATH=/usr/bin:/usr/local/bin
Environment=NODE_ENV=production
WorkingDirectory=/opt/linkfinds
[Install]
WantedBy=multi-user.target
